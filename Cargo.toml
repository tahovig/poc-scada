[package]
name = "poc-scada"
version = "0.1.0"
edition = "2024"

[dependencies]
pcap-parser = { version = "0.17", features = ["data"] }
etherparse = "0.20"
clap = { version = "4", features = ["derive"] }
